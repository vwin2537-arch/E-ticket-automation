# AUDIT.md — เช็กลิสต์ตรวจสอบ + แก้ไข DNP E-Ticket Auto-fill

> รายงานตรวจสอบโค้ดทั้งโปรเจค (อ่านโค้ด + สวมบทเจ้าหน้าที่หน้าด่าน) — 3/6/69
> ไล่แก้ทีละข้อ แก้เสร็จติ๊ก `[x]` + เติมวันที่/วิธีแก้ในช่อง "แก้แล้ว"
> สถานะ: 🔴 ยังไม่แก้ · 🟡 กำลังทำ · ✅ แก้แล้ว

## วิธีใช้ไฟล์นี้
- พี่วินบอก "แก้ข้อ 1" → มุกแก้โค้ด + กลับมาติ๊ก `[x]` ข้อนั้น + เขียนใต้หัวข้อว่าแก้ยังไง/วันไหน
- พี่วินติ๊กเองก็ได้ (เปลี่ยน `[ ]` เป็น `[x]`)
- แก้ใหญ่ๆ เสร็จ มุกจะ sync ไป `PROGRESS.md` ด้วย (ของจริงอยู่ที่นั่น ไฟล์นี้คือ tracker เฉพาะกิจ)

---

## 🔴 P0 — แก้ด่วน (กันเงินหาย / เครื่องแฮงค์)

### [x] 1. โหมดทดสอบ (dryRun) ไปปิด "ใบจริงที่รอจ่ายเงิน" ทิ้ง ✅
- **ไฟล์:** `src/server.js:47-54`
- **อาการ:** จองใบจริงเสร็จ นทท.กำลังจ่าย → เผลอกด "โหมดทดสอบ" ลองใบถัดไป → ใบจริงถูก `prevTab.browser.close()` ปิดทันที เงินหาย
- **แนวทาง:** dryRun ไม่ควรแตะ `lastTab`/`prevTab` เลย — แยกเงื่อนไข ถ้า `dryRun` ไม่ปิดใบเก่า + ไม่ตั้ง lastTab
- **แก้แล้ว (3/6/69):** แยกตัวแปร `lastDryTab` ออกจาก `lastTab`. `prevTab = dryRun ? lastDryTab : lastTab` →
  จองทดสอบปิดเฉพาะใบทดสอบเก่า / จองจริงปิดเฉพาะใบจริงเก่า แยกขาดกัน. ทดสอบ/จริงไม่ทับกันแล้ว
  ใบจริงรอจ่ายปลอดภัยแม้กดทดสอบคั่น. ✅ syntax OK.
  ✅ **เทส server จริงผ่าน (3/6/69):** dryRun×2 ปิดถูกใบ + ยิงใบจริงค้างหน้าสรุปแล้วกด dryRun คั่น →
  นับ browser คงที่ 15 process (ใบจริงรอด ไม่โดนปิด) ยืนยันตรงบั๊ก P0 + พี่วินเห็นบนจอ

### [x] 2. ไม่มี graceful shutdown → chromium ค้างกิน RAM สะสม ✅
- **ไฟล์:** `src/server.js` (ไม่มี SIGINT/SIGTERM handler), `pool.js` มี `drain()` แต่ไม่มีใครเรียก
- **อาการ:** ปิด-เปิดโปรแกรมวันละหลายรอบ → chromium orphan ค้างทีละ 3-4 ตัว สะสม → เครื่องช้า/แฮงค์ตอนบ่ายคนเยอะ
- **แนวทาง:** `process.on('SIGINT'/'SIGTERM')` → `pool.drain()` + ปิด `lastTab` แล้ว `process.exit()`
- **แก้แล้ว (3/6/69):** เพิ่ม `shutdown(signal)` จับ SIGINT/SIGTERM → ปิด `lastTab`+`lastDryTab`+`pool.drain()` แล้ว exit.
  มี `shuttingDown` guard กันกดซ้ำ + safety timer บังคับออกใน 5 วิถ้า close ค้าง. ✅ syntax OK.
  ⚠️ Windows: Ctrl+C trigger ได้ / ปิดหน้าต่าง cmd ตรงๆ อาจไม่ trigger (best-effort).
  ✅ **เทส server จริง (Mac) ผ่าน (3/6/69):** SIGTERM ขณะมี 5 browser ค้าง (3 warm + ใบจริง + dryRun) →
  browser เหลือ 0 + port ปล่อย + log `Cleanup done. Bye.` (graceful แท้ ไม่โดน safety-timer force). ⏳ Windows ยังรอเทส

---

## 🟡 P1 — คุ้มค่าหน้างาน / กัน UX สับสน

### [x] 3. หลัง 15:30 warm pool อุ่น "วันนี้" ที่ปิดแล้ว → ขึ้น error แดงค้าง ✅ (ทำแบบฉลาด)
- **ไฟล์:** `config.js` `automation.js` `pool.js` `server.js` `public/index.html`
- **อาการ:** เลย cutoff แล้ว warmTab เลือกวันไม่ได้ (disabled) → lastError ค้าง → หน้ากากโชว์ "⚠️ อุ่นเครื่องไม่สำเร็จ" แดงทั้งที่ระบบปกติ
- **แนวทาง (พี่วินขออัพเกรด):** ไม่ใช่แค่หยุด warm — ให้ **เด้งไปจองพรุ่งนี้** เลยตอนเลย cutoff (เวลาตัด **15:20** รวม buffer)
- **แก้แล้ว (3/6/69):** เพิ่ม `bookDate()` (automation.js) เป็น "วันเป้าหมาย" = ก่อน 15:20 คืนวันนี้ / เลย 15:20 คืนพรุ่งนี้ /
  ข้ามเที่ยงคืนกลับวันนี้เอง. `pool.js` warm ตาม bookDate (แทน todayISO ตายตัว) + acquire ทิ้งแท็บที่ไม่ตรงวันเป้าหมาย.
  ย้าย `SLOT_CLOSE_BUFFER_MIN` ไป `config.js` (single source) ส่งให้ frontend ผ่าน `/api/config`.
  หน้ากาก default วัน = `bookDateLocal()` + แถบ warm เทียบ `bookDate` (โชว์ว่าเตรียมวันไหนไว้).
  ✅ ทดสอบ bookDate 8 เคสเวลา (08:00/14:00/15:19=วันนี้, 15:20/15:30/23:59=พรุ่งนี้, 00:00/00:01=วันนี้) ผ่านหมด
  + syntax ทุกไฟล์ผ่าน.
  ✅ **เทส server จริงผ่าน (3/6/69 เวลา 22:45 เลย cutoff):** `/api/pool-status` = `bookDate:2026-06-04` (พรุ่งนี้)
  + `ready:3/3` + `lastError:null` (ไม่มี error แดงค้าง) + frontend `bookDateLocal()` default หน้ากาก = 4 มิ.ย. (พี่วินเห็นบนจอ)

### [x] 4. `running` lock ไม่มี timeout ✅
- **ไฟล์:** `src/server.js:39`
- **อาการ:** ถ้า fillBooking ค้างนาน (DNP ไม่ตอบ) ปุ่มจองทั้งระบบล็อกจน Playwright timeout เอง (ไม่ deadlock ถาวร แต่หน้างานอึดอัด)
- **แนวทาง:** ใส่ timeout ครอบ fillBooking หรือ reset running ด้วย safety timer
- **แก้แล้ว (3/6/69):** เพิ่ม `withTimeout()` ครอบ `fillBooking` ด้วย `Promise.race` (timeout `BOOK_TIMEOUT_MS` = 2 นาที).
  เกินเวลา → โยน error ไทย → ลง catch/finally ปกติ (ปลด running + ปิด tab ค้างใน 60 วิ). เลือกครอบ fillBooking
  แทน "reset running เฉยๆ" เพราะ reset แล้วงานเก่ายังทำอยู่ คนกดซ้อนได้ = 2 งานชนกัน. ตั้ง 2 นาที — หน้างาน
  กลุ่มใหญ่สุด ~รถตู้ 20 คน (~60 วิ) กรุ๊ปใหญ่จองจากบ้าน + เผื่อ buffer กัน false timeout ตัดใบที่ใกล้สำเร็จ. ✅ syntax OK.
  ✅ **เทส server จริงผ่าน (3/6/69):** ลด `BOOK_TIMEOUT_MS`=3วิชั่วคราว ยิง dryRun 10 คน (กรอก >3วิ) →
  ตัว1 timeout error เด้งกลางคัน / ตัว2 ยิงซ้อนติด lock "มีงานกำลังทำอยู่" / ตัว3 หลัง timeout ผ่าน lock
  (= release-on-timeout ทำงาน ไม่ค้าง). พิสูจน์ lock 2 ทิศครบ. คืนค่า 2 นาที + restart แล้ว

### [x] 5. จองทีละใบ — เตรียมใบถัดไประหว่างคนแรกจ่ายไม่ได้ (pipeline หลายใบ) ✅
- **ไฟล์:** `src/config.js` `src/server.js` `public/index.html`
- **อาการ:** คิวยาว คนจ่ายช้า อยากเตรียมใบถัดไป แต่กดปุ๊บใบรอจ่ายโดนปิด
- **requirement (ถามพี่วิน 5 ข้อ 3/6/69):** ดัก 2-3 ใบ / เน้น จนท.สะดวก ไม่ห่วง นทท.เห็นจอ /
  ไม่ต้องกดปิดใบเก่าเอง / เครื่องด่าน RAM 8GB+ / ชนเพดานแล้ว **เตือน ไม่ปิดอัตโนมัติ** (ปลอดภัย)
- **แก้แล้ว (3/6/69):** เปลี่ยน `lastTab` (ใบเดียว) → `pendingTabs[]` (คิวใบจริง สูงสุด `MAX_PENDING`=3).
  จองใบใหม่สำเร็จ = **push เข้าคิว ไม่ปิดใบเก่า** (กันเงินหาย). กลไกหลุดคิว: `prunePending()` กรอง
  `browser.isConnected()` → จนท.ปิดหน้าต่างใบที่จ่ายเสร็จ ระบบเอาออกจากคิวเอง (**ไม่ต้องกดปิดในระบบ**).
  ครบเพดาน = return error เตือน ไม่ acquire ไม่ปิดอะไร. dryRun แยก (`lastDryTab` ใบเดียว ปิดอันเก่า ไม่เข้าคิว).
  `/api/pool-status` เพิ่ม `pending`/`maxPending` → หน้ากากโชว์แถบ "🎫 ดักอยู่ N/3 ใบ". shutdown ปิดทั้งคิว.
  ✅ **เทส `scripts/test-pipeline.js` 16/16 ผ่าน** (mock pool+fillBooking ไม่ยิง DNP): ไม่ปิดใบเก่า /
  เพดานเตือน / prune หลุดคิว / dryRun ไม่ปนใบจริง / pool-status ถูก. ⏳ รอเทส Windows จริงที่ด่าน

### [ ] 6. กรอกผิดต้องเริ่มใหม่ทั้งใบ
- **อาการ:** เห็นยอดผิดตอนหน้าสรุป แก้ไม่ได้ ต้องล้างกรอกใหม่หมด
- **แนวทาง:** (รอตัดสินใจ) — อาจเพิ่มยืนยันสรุปก่อนกด/ปุ่มย้อน
- **แก้แล้ว:** _(ยังไม่แก้)_

### [ ] 7. เน็ตล่มกลางคัน = error เด้ง เริ่มใหม่หมด (ไม่มี auto-retry)
- **แนวทาง:** retry อัตโนมัติ 1-2 ครั้งก่อนยอมแพ้
- **แก้แล้ว:** _(ยังไม่แก้)_

### [ ] 8. ถ้าด่านมีหลายช่องขายบัตร — ระบบออกแบบมาเครื่อง/session เดียว
- **ต้องรู้ก่อน:** ด่านมีกี่ช่องขาย? ถ้าหลายเครื่องชี้ session เดียวพร้อมกัน token อาจชนกัน
- **แก้แล้ว:** _(รอข้อมูลจากพี่วิน)_

---

## 🟢 P2 — ฟีเจอร์เสริม / ระยะยาว

### [ ] 9. Preset กลุ่มยอดฮิต (เช่น "ครอบครัวไทย 2+2+รถ" กดทีเดียว)
### [ ] 10. เสียง/แจ้งเตือนเมื่อกรอกเสร็จ (ละสายตาไปรับเงินได้)
### [ ] 11. สรุปยอดวันนี้บนหน้ากาก (กี่ใบ/กี่คน/ยอดรวม ไม่ต้องเปิด Excel)
### [ ] 12. Smoke test รันเช้าทุกวัน — เช็กว่ายังกรอกได้ (กัน DNP เปลี่ยนหน้าเว็บแล้วพังเงียบ)
### [ ] 13. ปฏิทินเทียบปี ค.ศ. — ถ้า DNP เปลี่ยนเป็น พ.ศ. จะคลิกผิดวัน (`datepicker.js`) เก็บเป็นข้อควรระวัง

---

## ข้อสังเกตเชิงระบบ (ไม่ใช่ todo แต่ควรรู้)
- ผูกกับ DOM/ข้อความ DNP 100% → DNP อัพเดทหน้าเว็บเมื่อไหร่พังเงียบ (เกี่ยวกับข้อ 12)
- Session เดียว = จุดตายเดียว หมดอายุทั้งด่านจองไม่ได้ (ใครที่ด่าน login เองได้บ้าง?)
